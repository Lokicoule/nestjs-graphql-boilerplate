import { SettingsReadingResolver } from './settings-reading.resolver';
import { SettingsWritingResolver } from './settings-writing.resolver';

export const settingsResolvers = [
  SettingsReadingResolver,
  SettingsWritingResolver,
];
